select now();

create table "users"(
  "name" varchar,
  "age" int
);

insert into users values('eunsu', '23');

select * from users;

update users set age=30 where name='eunsu';

delete from users;

